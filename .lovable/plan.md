## Changement du libellé de navigation

Remplacer le texte du lien de navigation `Création de sites web` par `Web` dans le composant Header (`src/components/Header.tsx`, ligne 8).

Aucun autre changement requis.